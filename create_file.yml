---
- name: this playbook creates a file or directory
  hosts: all
    # become: true
    #  gather_facts: no
  tasks:
    - name: Creating a file
      file:
        path: /home/ansadmin/dir1
        state: directory
